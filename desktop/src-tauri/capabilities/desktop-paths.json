{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-paths",
  "description": "The main desktop window may open fixed application directories, enter data maintenance, and explicitly enable or disable local plugin discovery.",
  "windows": ["main"],
  "permissions": [
    "allow-open-desktop-directory",
    "allow-open-data-maintenance",
    "allow-get-plugin-integration-status",
    "allow-set-plugin-integration-enabled"
  ]
}
